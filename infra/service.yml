apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: project-image-prod-yml
  namespace: favorable-tree-471508-t9
  labels:
    cloudcontainers-googleapis-com-container-name: project-image-prod
spec:
  template:
    spec:
      serviceAccountName: image-app@favorable-tree-471508-t9.iam.gserviceaccount.com
      containers:
        - image: IMAGE_NAME
          ports:
            - containerPort: 8080
          resources:
            limits:
              cpu: "1000m"
              memory: "512Mi"
